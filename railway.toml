[build]
builder = "nixpacks"

[[services]]
name = "api"
startCommand = "uvicorn adwatch.api:app --host 0.0.0.0 --port $PORT"

[[services]]
name = "dashboard"
startCommand = "streamlit run dashboard.py --server.port $PORT --server.address 0.0.0.0 --server.headless true"
